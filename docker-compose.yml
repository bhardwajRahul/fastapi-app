services:
  app-api:
    build:
      context: .
      dockerfile: Dockerfile.web
    restart: always
    ports:
      - "8000:8000"
    depends_on:
      - postgres
    environment:
      - DATABASE_URL=postgresql+asyncpg://app:app@postgres:5432/app
      # set to the reverse proxy ip range so per-ip rate limiting trusts x-forwarded-for
      # - FORWARDED_ALLOW_IPS=10.0.0.0/8
    volumes:
      - .:/app

  postgres:
    image: postgres:17
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=app
      - POSTGRES_PASSWORD=app
      - POSTGRES_DB=app
      - TZ=UTC
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
